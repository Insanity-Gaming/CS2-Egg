# Force LF for files that run on or are consumed by Linux
*.sh    text eol=lf
*.json  text eol=lf
*.yml   text eol=lf

# README and gitignore can follow platform convention
*.md    text
.gitignore text
